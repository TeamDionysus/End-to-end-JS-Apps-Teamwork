.container
    .row
        .col-md-4
            input.form-control(type="text", placeholder="Search for item...", ng-model="query")
            .pull-right
        button.btn.btn-primary(ng-click="findItem(query)", ng-disabled="profileForm.$invalid") Search
        .col-md-2 Sort
        .col-md-4
            select.form-control(ng-model="sort")
                option(value=" " ng-selected="'true'") Default
                option(value="title") Title
                option(value="published") Published
    br
    table.table
        tr
            th Title
            th Published
        tr(ng-repeat="course in courses | filter: search | orderBy: sort")
            td
                a(href="#/courses/{{ course._id }}") {{ course.title }}
            td {{ course.published | date }}